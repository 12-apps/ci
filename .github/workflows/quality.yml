name: Quality (reusable)

# Reusable static-quality + test-reliability gate. Consuming repos add a small caller:
#
#   jobs:
#     quality:
#       needs: changes
#       if: needs.changes.outputs.code == 'true'
#       uses: 12-apps/ci/.github/workflows/quality.yml@v1
#       with:
#         pre-e2e-command: pnpm --filter @repo/shared-helpers build
#       secrets: inherit
#
# Runs in the CALLER's checkout, so it uses the consumer repo's own
# eslint quality configs, scripts/, and `quality*` package.json scripts.
# The consumer must define these scripts: quality:complexity, quality:flakiness,
# quality:dup, quality:quarantine, quality:knip, test:e2e:reliability (see
# CONSUMING.md). All static gates run as sequential steps of one `static-gates`
# job (one runner + checkout + install), each guarded by `!cancelled()` so every
# gate still reports even after an earlier one fails; consumers' required-check
# identity is the reusable workflow's overall result.

on:
  workflow_call:
    inputs:
      node-version:
        description: 'Node version for all jobs.'
        type: string
        required: false
        default: '24'
      run-knip:
        description: 'Run knip (unused files/exports/deps) as a blocking gate — the consumer supplies a shrink-only ratchet (quality:knip) so it only fails on NEW dead code.'
        type: boolean
        required: false
        default: true
      run-data-views:
        description: >-
          Run the DataViews backend-only gate: every admin list rendered with
          the shared DataViews panel must be server-driven (filter/sort/
          paginate/search at the backend). Opt-in — the consumer supplies a
          `quality:data-views` script plus a shrink-only exceptions file that
          grandfathers existing client-mode lists, so it only fails on NEW
          client-side lists or grandfathered ones that regressed.
        type: boolean
        required: false
        default: false
      run-e2e-reliability:
        description: 'Re-run changed e2e specs N times to flush flakes.'
        type: boolean
        required: false
        default: true
      run-affected-e2e:
        description: 'Run ONLY the e2e specs affected by the diff (needs a test:e2e:affected script + selector/manifest in the consumer). Opt-in.'
        type: boolean
        required: false
        default: false
      flakiness-targets:
        description: 'Lint targets for the central flakiness gate (space-separated, relative to the repo root). Ignored when the consumer defines its own quality:flakiness script.'
        type: string
        required: false
        default: '.'
      pre-e2e-command:
        description: 'Shell run before the e2e reliability gate (e.g. build shared packages). Empty to skip.'
        type: string
        required: false
        default: ''
      install-playwright:
        description: 'Install Playwright browsers before the reliability gate.'
        type: boolean
        required: false
        default: true
      e2e-repeat:
        description: 'Times to re-run each changed e2e spec (retries stay 0).'
        type: string
        required: false
        default: '3'
      e2e-spec-pattern:
        description: >-
          Extended regex (grep -E) matching e2e spec paths for the reliability
          job's early exit: when NO changed file matches, the job skips its
          toolchain setup entirely instead of discovering "nothing to re-run"
          after ~60s of installs. Keep it a SUPERSET of the consumer selector's
          own filter — a false positive only costs the setup (the consumer
          script still decides precisely); a narrower pattern would silently
          skip real re-runs. Default covers both spec conventions
          (tests/e2e/*.spec.ts and co-located *.e2e.ts).
        type: string
        required: false
        default: '(^|/)tests/e2e/.+\.spec\.[jt]sx?$|\.e2e\.[jt]sx?$'
      nextjs-app-dirs:
        description: >-
          Comma-separated Next.js App Router roots (e.g. "apps/web/app") to
          enforce navigation-feedback coverage on: every route segment shipping a
          page.* must ship a sibling loading.*. Empty (default) skips the gate.
        type: string
        required: false
        default: ''
      loading-must-render:
        description: >-
          Optional component name every loading.* must reference (e.g. a shared
          RouteLoading spinner) so coverage cannot be satisfied by an empty
          placeholder file. Empty skips the content check.
        type: string
        required: false
        default: ''
      e2e-coverage-app-dirs:
        description: >-
          Comma-separated Next.js App Router roots (e.g. "apps/web/app") to
          enforce e2e page↔spec coverage on: every route segment shipping a
          page.* must have a co-located e2e spec (default suffix .e2e.ts) in its
          subtree, and every such spec must sit under a route segment. Pages
          grandfathered in the exceptions file are skipped. Empty (default)
          skips the gate.
        type: string
        required: false
        default: ''
      e2e-coverage-spec-suffix:
        description: 'Filename suffix marking a co-located e2e spec for the coverage gate.'
        type: string
        required: false
        default: '.e2e.ts'
      e2e-coverage-page-file:
        description: >-
          Regex (matched against a file basename) that marks a directory as a
          route page. Default is the Next.js App Router convention (page.tsx). A
          Vite/React-Router SPA that keys routes on src/pages/<route>/index.tsx
          would pass "^index\.tsx$".
        type: string
        required: false
        default: '^page\.(js|jsx|ts|tsx)$'
      e2e-coverage-exempt-globs:
        description: >-
          Comma-separated filename substrings that exempt a co-located spec from
          the spec→page (no-orphan) check — cross-cutting specs that are not tied
          to a single page. Default covers journey and global specs.
        type: string
        required: false
        default: '.journey.,.global.'
      e2e-coverage-exceptions:
        description: >-
          Repo-root-relative path to a shrink-only JSON array of route-segment
          dirs grandfathered as having no e2e spec yet. The list may only shrink,
          and touching any file under a listed segment forces it to be de-listed
          (i.e. get a spec). A missing file means no grandfathering.
        type: string
        required: false
        default: '.e2e-coverage-exceptions.json'
      e2e-content-app-dirs:
        description: >-
          Comma-separated roots (e.g. "apps/web/app" or "apps/admin/src/pages")
          to enforce e2e content-quality on. This is the companion to the
          page↔spec coverage gate: coverage proves a spec FILE exists, this gate
          proves the spec actually EXERCISES the page rather than being a
          navigate-and-toBeVisible render smoke test. Pure static heuristics — no
          browser/e2e infra. Every co-located spec (default suffix .e2e.ts) must
          (1) perform ≥1 interaction, (2) carry ≥1 behavioral assertion (not just
          toBeVisible-style render checks), (3) have ≥1 assertion AFTER an
          interaction (verify a state change), and (4) have ≥ min assertions.
          Weak specs are grandfathered in a shrink-only exceptions file. Empty
          (default) skips the gate. HEURISTIC, NOT PROOF: it stops lazy specs,
          not adversarial ones — real teeth come from runtime coverage.
        type: string
        required: false
        default: ''
      e2e-content-spec-suffix:
        description: 'Filename suffix marking a co-located e2e spec for the content gate.'
        type: string
        required: false
        default: '.e2e.ts'
      e2e-content-exempt-globs:
        description: >-
          Comma-separated filename substrings that exempt a spec from the content
          gate — cross-cutting specs (journeys/globals) whose shape the heuristics
          are not tuned for. Default matches the coverage gate.
        type: string
        required: false
        default: '.journey.,.global.'
      e2e-content-min-assertions:
        description: >-
          Minimum number of assertions (expect matchers) a non-exempt spec must
          carry. A weak floor on top of the interaction / behavioral-assertion /
          assert-after-action rules (which do the real work). Kept at 2 so a
          legitimate two-assertion flow (e.g. delete → assert gone + assert count)
          is not a false positive; raise it for a stricter bar.
        type: number
        required: false
        default: 2
      e2e-content-exceptions:
        description: >-
          Repo-root-relative path to a shrink-only JSON array of spec file paths
          grandfathered as content-weak. The list may only shrink, and touching a
          listed spec forces it to be de-listed (i.e. strengthened). A missing
          file means no grandfathering.
        type: string
        required: false
        default: '.e2e-content-exceptions.json'

# Least-privilege, enforced at the source: every job here only reads the repo.
# Capping permissions at the workflow level means a consumer that grants more in
# a combined CI file (e.g. packages: write, actions: write) cannot leak that
# privilege into this reusable workflow — the effective grant is the intersection.
permissions:
  contents: read

jobs:
  # All static-analysis gates run as sequential steps of this single job: one
  # runner spin-up + checkout + pnpm install instead of ten. Every gate step is
  # guarded by `!cancelled()` so a failing gate never hides the gates after it
  # (full inventory, like the old parallel jobs) while the job still ends
  # failed if any gate failed; consumers' required-check identity is the
  # reusable workflow's overall result.
  static-gates:
    name: Static Gates
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # Full history: several gates diff the branch against its merge base.
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
        with:
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node-version }}
          cache: 'pnpm'
      - name: Install dependencies
        id: install
        shell: bash
        run: |
          set -o pipefail
          pnpm install --frozen-lockfile 2>&1 | tee install.log
      - name: Complexity gate (size/complexity on source)
        if: ${{ !cancelled() && steps.install.outcome == 'success' }}
        shell: bash
        run: |
          set -o pipefail
          pnpm quality:complexity 2>&1 | tee complexity.log

      # Flakiness: the anti-flake ruleset + baseline runner + eslint toolchain
      # live ONLY in 12-apps/ci (the `flakiness-lint` composite action). The
      # central action is used by default, so updating the rules here reaches
      # every consumer on its next CI run — the consumer keeps only its
      # repo-specific eslint-suppressions.json baseline, not the
      # config/runner/devDeps.
      #
      # Back-compat: a consumer that still defines its own `quality:flakiness`
      # script keeps running that (legacy path) until it migrates by deleting
      # the script.
      - name: Detect flakiness gate source
        id: src
        if: ${{ !cancelled() && steps.install.outcome == 'success' }}
        shell: bash
        run: |
          set -o pipefail
          if node -e "const s=(require('./package.json').scripts||{});process.exit(s['quality:flakiness']?0:1)" 2>/dev/null; then
            echo "mode=legacy" >> "$GITHUB_OUTPUT"
          else
            echo "mode=central" >> "$GITHUB_OUTPUT"
          fi
      - name: Flakiness gate (legacy consumer script)
        if: ${{ !cancelled() && steps.install.outcome == 'success' && steps.src.outputs.mode == 'legacy' }}
        shell: bash
        run: |
          set -o pipefail
          pnpm quality:flakiness 2>&1 | tee flakiness.log
      - name: Fetch base ref (central gate diffs against it)
        if: ${{ !cancelled() && steps.install.outcome == 'success' && steps.src.outputs.mode == 'central' }}
        shell: bash
        run: git fetch --no-tags origin "${GITHUB_BASE_REF:-main}"
      - name: Flakiness gate (central 12-apps/ci ruleset)
        if: ${{ !cancelled() && steps.install.outcome == 'success' && steps.src.outputs.mode == 'central' }}
        uses: 12-apps/ci/.github/actions/flakiness-lint@v1
        with:
          node-version: ${{ inputs.node-version }}
          targets: ${{ inputs.flakiness-targets }}
      - name: Copy-paste detection (jscpd)
        if: ${{ !cancelled() && steps.install.outcome == 'success' }}
        shell: bash
        run: |
          set -o pipefail
          pnpm quality:dup 2>&1 | tee jscpd.log
      - name: Validate flaky-quarantine.json
        if: ${{ !cancelled() && steps.install.outcome == 'success' }}
        shell: bash
        run: |
          set -o pipefail
          pnpm quality:quarantine 2>&1 | tee quarantine.log
      - name: Unused files/exports/deps (knip)
        if: ${{ !cancelled() && steps.install.outcome == 'success' && inputs.run-knip }}
        shell: bash
        run: |
          set -o pipefail
          pnpm quality:knip 2>&1 | tee knip.log
      # DataViews backend-only ratchet (opt-in): lists rendered with the shared
      # DataViews panel must be server-driven. Client-mode consumers are
      # grandfathered in the repo's exceptions file and the list can only
      # shrink — a new client-side list, or a grandfathered one that regressed,
      # fails here.
      - name: DataViews backend-only gate
        if: ${{ !cancelled() && steps.install.outcome == 'success' && inputs.run-data-views }}
        shell: bash
        run: |
          set -o pipefail
          pnpm quality:data-views 2>&1 | tee data-views-gate.log

      # Enforce the `.quality-exceptions` burn-down policy on every PR:
      #   (A) shrink-only — the file may LOSE lines, never gain them, so a new
      #       file can never be grandfathered (fix it instead of listing it);
      #   (B) touch-must-fix — a changed file that is STILL listed must be
      #       removed from the list, at which point the complexity/flakiness
      #       gate makes its findings hard errors. So you cannot touch a
      #       grandfathered file and leave it grandfathered.
      # Self-contained (pure git + bash); needs no consumer script. No-ops when
      # the repo has no `.quality-exceptions` file.
      - name: Enforce .quality-exceptions burn-down (shrink-only + touch-must-fix)
        if: ${{ !cancelled() && steps.install.outcome == 'success' }}
        shell: bash
        env:
          BASE_REF: ${{ github.base_ref || 'main' }}
        run: |
          set -o pipefail
          if [ ! -f .quality-exceptions ]; then
            echo "No .quality-exceptions file — nothing to enforce."
            exit 0
          fi
          # Fail-fast on the diff baseline (never masked): we must have the base.
          git fetch --no-tags origin "$BASE_REF"
          merge_base="$(git merge-base "origin/$BASE_REF" HEAD)" || {
            echo "::error::Could not compute merge-base with origin/$BASE_REF"
            exit 1
          }

          fail=0

          # (A) Shrink-only: any ADDED non-comment line in .quality-exceptions is a
          # new grandfather entry — forbidden.
          added="$(
            git diff "$merge_base" HEAD -- .quality-exceptions \
              | grep -E '^\+' | grep -vE '^\+\+\+' | sed -E 's/^\+//' \
              | sed -E 's/[[:space:]]+$//' | grep -vE '^\s*(#|$)' || true
          )"
          if [ -n "$added" ]; then
            echo "::error::.quality-exceptions may only shrink, never grow. Lines ADDED:"
            printf '%s\n' "$added" | sed 's/^/  + /'
            echo "You cannot grandfather a file — fix its issues instead of listing it."
            fail=1
          fi

          # (B) Touch-must-fix: a changed file that is still listed must be
          # de-listed (removing its line makes the eslint gate enforce its fixes).
          # Use --name-status -M (not --name-only) so a RENAMED or DELETED
          # grandfathered file is caught too: .quality-exceptions holds the OLD
          # path, but --name-only reports only a rename's NEW path — so the old
          # listed path would never match and the gate would silently pass,
          # leaving the file grandfathered. Here we check BOTH sides of a rename
          # (old + new) and the path of a delete.
          # `|| true` because grep exits 1 when it selects no lines, and a
          # consumer that has BURNED THE LIST DOWN has exactly that: a file left
          # holding only comments (or nothing). GitHub runs `shell: bash` as
          # `bash -e -o pipefail`, so without this the assignment inherits grep's
          # 1 and the step dies right here — before either check runs, and before
          # either has emitted its ::error::, so the log shows only a bare
          # "Process completed with exit code 1". Finishing the burn-down would
          # look like a gate failure with no stated reason. Same guard as (A) and
          # the offenders roll-up below, for the same reason.
          listed_now="$(grep -vE '^\s*(#|$)' .quality-exceptions | sed -E 's/[[:space:]]+$//' || true)"
          offenders=""
          while IFS=$'\t' read -r status p1 p2; do
            [ -z "$status" ] && continue
            case "$status" in
              C*) involved="$p2" ;;            # copy: only the new file is new work
              R*) involved="$p1"$'\n'"$p2" ;;  # rename: BOTH old (source) and new
              *)  involved="$p1" ;;            # A / M / D: the single path
            esac
            while IFS= read -r f; do
              [ -z "$f" ] && continue
              if printf '%s\n' "$listed_now" | grep -qxF "$f"; then
                offenders="$offenders$f"$'\n'
              fi
            done <<< "$involved"
          done < <(git diff --name-status -M --diff-filter=ACMRD "$merge_base" HEAD)
          offenders="$(printf '%s' "$offenders" | grep -vE '^[[:space:]]*$' | sort -u || true)"
          if [ -n "$offenders" ]; then
            echo "::error::You changed files still grandfathered in .quality-exceptions (a rename or delete counts as touching):"
            printf '%s\n' "$offenders" | sed 's/^/  - /'
            echo "Policy: a touched file must be removed from .quality-exceptions AND have"
            echo "all its complexity/flakiness issues fixed. For a rename, remove the OLD"
            echo "path's line and clean up the new file; for a delete, remove the stale line."
            fail=1
          fi

          if [ "$fail" -ne 0 ]; then exit 1; fi
          echo "✓ .quality-exceptions ratchet OK (shrink-only; no touched file left grandfathered)."

      # In the Next.js App Router, a route segment that ships a page.* without
      # a loading.* renders NOTHING while the segment streams (prod) or
      # compiles on demand (dev) — the user stares at a frozen screen. This
      # gate enforces that every page has instant loading feedback. It only
      # walks the checkout. Opt in with nextjs-app-dirs; optionally require
      # each loading.* to reference a shared spinner via loading-must-render
      # (an existence-only check could be satisfied by an empty placeholder).
      - name: Enforce page.* ↔ loading.* coverage
        if: ${{ !cancelled() && steps.install.outcome == 'success' && inputs.nextjs-app-dirs != '' }}
        env:
          APP_DIRS: ${{ inputs.nextjs-app-dirs }}
          MUST_RENDER: ${{ inputs.loading-must-render }}
        shell: bash
        run: |
          set -o pipefail
          node <<'EOF'
          const { readdirSync, readFileSync } = require("node:fs");
          const { join } = require("node:path");
          const PAGE = /^page\.(js|jsx|ts|tsx)$/;
          const LOADING = /^loading\.(js|jsx|ts|tsx)$/;
          // api/ segments have no UI; node_modules is never a route. (_private
          // and dot folders are excluded separately in the directory loop.)
          const SKIP = new Set(["api", "node_modules"]);
          // Comma-only: paths may contain spaces, so comma is the separator.
          const roots = process.env.APP_DIRS.split(",").map((r) => r.trim()).filter(Boolean);
          const mustRender = process.env.MUST_RENDER || "";
          const failures = [];
          let pages = 0;
          function walk(dir) {
            let entries;
            try {
              entries = readdirSync(dir, { withFileTypes: true });
            } catch {
              failures.push(`${dir}: unreadable — check the nextjs-app-dirs input`);
              return;
            }
            const files = entries.filter((e) => e.isFile()).map((e) => e.name);
            const page = files.find((f) => PAGE.test(f));
            if (page) {
              pages += 1;
              const loading = files.find((f) => LOADING.test(f));
              if (!loading) {
                failures.push(
                  `${join(dir, page)}: no sibling loading.* — this segment renders nothing while it streams/compiles`,
                );
              } else if (
                mustRender &&
                !readFileSync(join(dir, loading), "utf8").includes(mustRender)
              ) {
                failures.push(`${join(dir, loading)}: does not reference ${mustRender}`);
              }
            }
            for (const e of entries) {
              if (!e.isDirectory()) continue;
              if (SKIP.has(e.name) || e.name.startsWith("_") || e.name.startsWith(".")) continue;
              walk(join(dir, e.name));
            }
          }
          roots.forEach(walk);
          // Flush failures before the zero-pages exit so unreadable-directory
          // diagnostics are not swallowed by the generic "no page.*" error.
          for (const f of failures) console.error(`::error::${f}`);
          if (pages === 0) {
            // Zero pages means the input points at the wrong place — fail loud
            // rather than green-lighting a gate that inspected nothing.
            console.error(`::error::no page.* found under: ${roots.join(" ")} — check nextjs-app-dirs`);
            process.exit(1);
          }
          if (failures.length > 0) {
            console.error(
              `${failures.length} of ${pages} route segment(s) lack loading feedback. ` +
                "Add a loading.tsx beside each page" +
                (mustRender ? ` rendering ${mustRender}.` : "."),
            );
            process.exit(1);
          }
          console.log(`OK: ${pages} route segment(s) covered by loading.* under: ${roots.join(" ")}`);
          EOF

      # E2E page↔spec coverage (opt-in): every route segment shipping a page.*
      # must have a co-located e2e spec (default *.e2e.ts) in its subtree, and
      # every such spec must sit under a route segment (cross-cutting
      # journey/global specs are exempt). Pages with no spec yet are
      # grandfathered in a shrink-only exceptions file: the list may only lose
      # entries, and touching any file under a listed segment forces that
      # segment to be de-listed (i.e. get a spec). Walks the checkout + diffs
      # against the base.
      - name: Enforce page.* ↔ e2e spec coverage
        if: ${{ !cancelled() && steps.install.outcome == 'success' && inputs.e2e-coverage-app-dirs != '' }}
        env:
          APP_DIRS: ${{ inputs.e2e-coverage-app-dirs }}
          SPEC_SUFFIX: ${{ inputs.e2e-coverage-spec-suffix }}
          EXEMPT: ${{ inputs.e2e-coverage-exempt-globs }}
          EXCEPTIONS_FILE: ${{ inputs.e2e-coverage-exceptions }}
          PAGE_FILE: ${{ inputs.e2e-coverage-page-file }}
          BASE_REF: ${{ github.base_ref || 'main' }}
        shell: bash
        run: |
          set -o pipefail
          node <<'EOF'
          const { readdirSync, readFileSync, existsSync } = require("node:fs");
          const { join, dirname } = require("node:path");
          // The route-page marker is configurable (default: Next.js page.*);
          // a Vite/React-Router SPA passes e.g. "^index\\.tsx$".
          let PAGE;
          try {
            PAGE = new RegExp(process.env.PAGE_FILE || "^page\\.(js|jsx|ts|tsx)$");
          } catch (e) {
            console.error(`::error::e2e-coverage-page-file is not a valid regex: ${e.message}`);
            process.exit(1);
          }
          const SKIP = new Set(["api", "node_modules"]);
          const norm = (p) => p.replace(/\/+$/, "");
          const under = (p, base) => p === base || p.startsWith(norm(base) + "/");
          const roots = process.env.APP_DIRS.split(",").map((r) => r.trim()).filter(Boolean);
          const SUFFIX = process.env.SPEC_SUFFIX || ".e2e.ts";
          const EXEMPT = (process.env.EXEMPT || "").split(",").map((s) => s.trim()).filter(Boolean);
          const exFile = process.env.EXCEPTIONS_FILE || "";

          let exceptions = [];
          if (exFile && existsSync(exFile)) {
            try {
              exceptions = JSON.parse(readFileSync(exFile, "utf8"));
            } catch (e) {
              console.error(`::error::${exFile} is not valid JSON: ${e.message}`);
              process.exit(1);
            }
            if (!Array.isArray(exceptions)) {
              console.error(`::error::${exFile} must be a JSON array of route-segment dirs`);
              process.exit(1);
            }
          }
          const exSet = new Set(exceptions.map(norm));

          const pageDirs = [];
          const specFiles = [];
          let traversalFailed = false;
          function walk(dir) {
            let entries;
            try {
              entries = readdirSync(dir, { withFileTypes: true });
            } catch {
              console.error(`::error::${dir}: unreadable — check e2e-coverage-app-dirs`);
              traversalFailed = true;
              return;
            }
            const files = entries.filter((e) => e.isFile()).map((e) => e.name);
            if (files.some((f) => PAGE.test(f))) pageDirs.push(norm(dir));
            for (const f of files) if (f.endsWith(SUFFIX)) specFiles.push(join(dir, f));
            for (const e of entries) {
              if (!e.isDirectory()) continue;
              if (SKIP.has(e.name) || e.name.startsWith("_") || e.name.startsWith(".")) continue;
              walk(join(dir, e.name));
            }
          }
          roots.forEach(walk);

          // A single unreadable root must fail the gate even when other roots
          // contain pages — otherwise that tree ships with no coverage enforced.
          if (traversalFailed) {
            console.error("::error::one or more e2e-coverage-app-dirs roots were unreadable (see above) — refusing to pass with an un-scanned tree.");
            process.exit(1);
          }

          if (pageDirs.length === 0) {
            console.error(`::error::no page.* found under: ${roots.join(", ")} — check e2e-coverage-app-dirs`);
            process.exit(1);
          }

          const pageDirSet = new Set(pageDirs);
          const isCovered = (pd) => specFiles.some((sf) => under(sf, pd));
          const failures = [];

          // (1) page → spec: every page has a co-located spec, unless grandfathered.
          for (const pd of pageDirs) {
            if (isCovered(pd) || exSet.has(pd)) continue;
            failures.push(`${pd}: page.* has no ${SUFFIX} spec in its subtree — add one, or grandfather it in ${exFile}`);
          }

          // (2) exceptions must stay honest: a listed dir that no longer exists as
          // a page, or that now HAS a spec, must be removed (drives the shrink).
          for (const ex of exSet) {
            if (!pageDirSet.has(ex)) {
              failures.push(`${exFile}: "${ex}" is not a route segment with a page.* — remove it`);
            } else if (isCovered(ex)) {
              failures.push(`${exFile}: "${ex}" now has a ${SUFFIX} spec — remove it (the list only shrinks)`);
            }
          }

          // (3) spec → page: no orphan specs (journey/global specs are exempt).
          const isExempt = (f) => EXEMPT.some((x) => f.includes(x));
          const hasAncestorPage = (dir) => {
            let d = norm(dir);
            while (roots.some((r) => under(d, r))) {
              if (pageDirSet.has(d)) return true;
              const parent = norm(dirname(d));
              if (parent === d) break;
              d = parent;
            }
            return false;
          };
          for (const sf of specFiles) {
            if (isExempt(sf)) continue;
            if (!hasAncestorPage(dirname(sf))) {
              failures.push(`${sf}: e2e spec is not under any route segment (no page.* at or above it) — tie it to a page, or mark it ${EXEMPT.join("/") || "exempt"}`);
            }
          }

          for (const f of failures) console.error(`::error::${f}`);
          if (failures.length > 0) {
            console.error(`${failures.length} e2e page↔spec coverage problem(s).`);
            process.exit(1);
          }
          console.log(`OK: ${pageDirs.length} route segment(s), ${specFiles.length} co-located ${SUFFIX} spec(s); ${exSet.size} grandfathered.`);
          EOF
      - name: Enforce exceptions ratchet (shrink-only + touch-must-fix)
        if: ${{ !cancelled() && steps.install.outcome == 'success' && inputs.e2e-coverage-app-dirs != '' }}
        env:
          APP_DIRS: ${{ inputs.e2e-coverage-app-dirs }}
          SPEC_SUFFIX: ${{ inputs.e2e-coverage-spec-suffix }}
          EXEMPT: ${{ inputs.e2e-coverage-exempt-globs }}
          EXCEPTIONS_FILE: ${{ inputs.e2e-coverage-exceptions }}
          PAGE_FILE: ${{ inputs.e2e-coverage-page-file }}
          BASE_REF: ${{ github.base_ref || 'main' }}
        shell: bash
        run: |
          set -o pipefail
          if [ -z "$EXCEPTIONS_FILE" ] || [ ! -f "$EXCEPTIONS_FILE" ]; then
            echo "No $EXCEPTIONS_FILE — nothing to ratchet."
            exit 0
          fi
          git fetch --no-tags origin "$BASE_REF"
          merge_base="$(git merge-base "origin/$BASE_REF" HEAD)" || {
            echo "::error::Could not compute merge-base with origin/$BASE_REF"
            exit 1
          }
          # Base version of the exceptions file. Distinguish "absent at base"
          # (BASE_EXISTED=0 → this PR is establishing the initial grandfather
          # baseline, so shrink-only must NOT reject the seeded entries) from
          # "present and empty at base" (BASE_EXISTED=1 → enforce shrink-only).
          if git show "$merge_base:$EXCEPTIONS_FILE" > /tmp/e2e-cov-base.json 2>/dev/null; then
            BASE_EXISTED=1
          else
            echo "[]" > /tmp/e2e-cov-base.json
            BASE_EXISTED=0
          fi
          BASE_JSON=/tmp/e2e-cov-base.json BASE_EXISTED="$BASE_EXISTED" HEAD_FILE="$EXCEPTIONS_FILE" MERGE_BASE="$merge_base" node <<'EOF'
          const { readFileSync } = require("node:fs");
          const { execSync } = require("node:child_process");
          const norm = (p) => String(p).replace(/\/+$/, "");
          const load = (f) => {
            try { const v = JSON.parse(readFileSync(f, "utf8")); return Array.isArray(v) ? v.map(norm) : []; }
            catch { return []; }
          };
          const base = new Set(load(process.env.BASE_JSON));
          const head = load(process.env.HEAD_FILE);
          let fail = false;

          // (A) shrink-only: no entry may be ADDED vs base — EXCEPT on the PR
          // that first creates the file, which establishes the initial
          // grandfather baseline (there is nothing to "grow" yet).
          if (process.env.BASE_EXISTED === "1") {
            const added = head.filter((p) => !base.has(p));
            if (added.length) {
              console.error("::error::" + process.env.HEAD_FILE + " may only shrink, never grow. Added:");
              added.forEach((p) => console.error(`  + ${p}`));
              console.error("You cannot grandfather a page — add its e2e spec instead of listing it.");
              fail = true;
            }
          } else {
            console.log(`(initial ${process.env.HEAD_FILE} — establishing the grandfather baseline; shrink-only enforced from the next PR.)`);
          }

          // (B) touch-must-fix: a changed file under a still-listed segment means
          // that segment must be de-listed (and thus get a spec). A rename/delete
          // counts as touching (check both sides).
          const listed = head; // still listed after this PR
          const mb = process.env.MERGE_BASE;
          const diff = execSync(`git diff --name-status -M --diff-filter=ACMRD ${mb} HEAD`, { encoding: "utf8" });
          const touched = new Set();
          for (const line of diff.split("\n")) {
            if (!line.trim()) continue;
            const parts = line.split("\t");
            const status = parts[0];
            const paths = status.startsWith("R") || status.startsWith("C") ? [parts[1], parts[2]] : [parts[1]];
            for (const p of paths) if (p) touched.add(p);
          }
          const offenders = listed.filter((seg) => [...touched].some((f) => f === seg || f.startsWith(seg + "/")));
          if (offenders.length) {
            console.error("::error::You changed files under segments still grandfathered in " + process.env.HEAD_FILE + " (a rename or delete counts as touching):");
            offenders.forEach((s) => console.error(`  - ${s}`));
            console.error("Policy: a touched segment must be removed from the exceptions file AND get a co-located e2e spec.");
            fail = true;
          }

          if (fail) process.exit(1);
          console.log(`✓ ${process.env.HEAD_FILE} ratchet OK (shrink-only; no touched segment left grandfathered).`);
          EOF

      # E2E content-quality (opt-in): the companion to e2e-coverage. Coverage
      # proves a spec FILE exists per page; this proves the spec actually
      # EXERCISES the page instead of being a navigate-and-toBeVisible render
      # smoke test. Pure static heuristics on the spec source — no browser/e2e
      # infra. A non-exempt spec must (1) perform ≥1 interaction
      # (click/fill/press/selectOption/…), (2) carry ≥1 behavioral assertion —
      # a matcher other than the render-only toBeVisible/toBeAttached/
      # toBeInViewport family (a positive toBeHidden or a negated visibility
      # check both prove a state change and count), (3) have ≥1 assertion AFTER
      # an interaction WITHIN THE SAME TEST (verify a state change, not just an
      # initial render), and (4) meet a min-assertion floor. HEURISTIC, NOT
      # PROOF — it stops lazy specs, not adversarial ones; real teeth come from
      # runtime coverage. Weak specs are grandfathered in a shrink-only
      # exceptions file with the same ratchet as e2e-coverage.
      - name: Enforce e2e spec content-quality
        if: ${{ !cancelled() && steps.install.outcome == 'success' && inputs.e2e-content-app-dirs != '' }}
        env:
          APP_DIRS: ${{ inputs.e2e-content-app-dirs }}
          SPEC_SUFFIX: ${{ inputs.e2e-content-spec-suffix }}
          EXEMPT: ${{ inputs.e2e-content-exempt-globs }}
          MIN_ASSERT: ${{ inputs.e2e-content-min-assertions }}
          EXCEPTIONS_FILE: ${{ inputs.e2e-content-exceptions }}
          BASE_REF: ${{ github.base_ref || 'main' }}
        shell: bash
        run: |
          set -o pipefail
          node <<'EOF'
          const { readdirSync, readFileSync, existsSync } = require("node:fs");
          const { join } = require("node:path");
          const norm = (p) => p.replace(/\/+$/, "");
          const SKIP = new Set(["api", "node_modules"]);
          const roots = process.env.APP_DIRS.split(",").map((r) => r.trim()).filter(Boolean);
          const SUFFIX = process.env.SPEC_SUFFIX || ".e2e.ts";
          const EXEMPT = (process.env.EXEMPT || "").split(",").map((s) => s.trim()).filter(Boolean);
          const MIN_ASSERT = Number.parseInt(process.env.MIN_ASSERT || "3", 10);
          const exFile = process.env.EXCEPTIONS_FILE || "";

          let exceptions = [];
          if (exFile && existsSync(exFile)) {
            try {
              exceptions = JSON.parse(readFileSync(exFile, "utf8"));
            } catch (e) {
              console.error(`::error::${exFile} is not valid JSON: ${e.message}`);
              process.exit(1);
            }
            if (!Array.isArray(exceptions)) {
              console.error(`::error::${exFile} must be a JSON array of spec file paths`);
              process.exit(1);
            }
          }
          const exSet = new Set(exceptions.map(norm));

          // Collect every co-located spec under the roots.
          const specFiles = [];
          let traversalFailed = false;
          function walk(dir) {
            let entries;
            try {
              entries = readdirSync(dir, { withFileTypes: true });
            } catch {
              console.error(`::error::${dir}: unreadable — check e2e-content-app-dirs`);
              traversalFailed = true;
              return;
            }
            for (const e of entries) {
              if (e.isFile()) {
                if (e.name.endsWith(SUFFIX)) specFiles.push(norm(join(dir, e.name)));
                continue;
              }
              if (!e.isDirectory()) continue;
              if (SKIP.has(e.name) || e.name.startsWith("_") || e.name.startsWith(".")) continue;
              walk(join(dir, e.name));
            }
          }
          roots.forEach(walk);

          // A single unreadable root must fail — never pass with an un-scanned tree.
          if (traversalFailed) {
            console.error("::error::one or more e2e-content-app-dirs roots were unreadable (see above) — refusing to pass with an un-scanned tree.");
            process.exit(1);
          }
          if (specFiles.length === 0) {
            console.error(`::error::no ${SUFFIX} spec found under: ${roots.join(", ")} — check e2e-content-app-dirs`);
            process.exit(1);
          }

          // ── Heuristics ──────────────────────────────────────────────────────
          // A known-matcher allowlist keeps plain JS methods (toString, toFixed,
          // toISOString, toLowerCase…) from being miscounted as assertions.
          // Render-only = "it's there on load". toBeHidden is deliberately NOT
          // here: asserting an element is hidden/absent (empty state, filtered
          // out, gated feature) is a state assertion, the positive twin of the
          // `.not.toBeVisible()` we already credit below.
          const RENDER_ONLY = new Set(["toBeVisible", "toBeAttached", "toBeInViewport"]);
          const KNOWN_MATCHERS = new Set([
            ...RENDER_ONLY, "toBeHidden",
            "toHaveText", "toContainText", "toHaveValue", "toHaveValues", "toHaveURL", "toHaveTitle",
            "toHaveCount", "toHaveAttribute", "toHaveClass", "toHaveId", "toHaveJSProperty", "toHaveCSS",
            "toBeChecked", "toBeEnabled", "toBeDisabled", "toBeEditable", "toBeEmpty", "toBeFocused",
            "toHaveScreenshot", "toHaveAccessibleName", "toHaveAccessibleDescription", "toHaveRole", "toBeOK",
            "toBe", "toEqual", "toStrictEqual", "toContain", "toContainEqual", "toMatch", "toMatchObject",
            "toMatchSnapshot", "toMatchInlineSnapshot", "toBeTruthy", "toBeFalsy", "toBeNull", "toBeNaN",
            "toBeDefined", "toBeUndefined", "toBeGreaterThan", "toBeGreaterThanOrEqual", "toBeLessThan",
            "toBeLessThanOrEqual", "toBeCloseTo", "toHaveLength", "toHaveProperty", "toThrow", "toThrowError",
            "toHaveBeenCalled", "toHaveBeenCalledTimes", "toHaveBeenCalledWith", "toHaveReturned",
          ]);
          const ACTION_RE = /\.\s*(click|dblclick|fill|type|press|pressSequentially|check|uncheck|setChecked|selectOption|selectText|setInputFiles|dragTo|tap|clear|hover|focus)\s*\(/g;
          // Optional `not.` prefix so a negated visibility check (e.g. asserting a
          // row disappeared after delete) counts as a behavioral assertion.
          const MATCHER_RE = /\.\s*(not\s*\.\s*)?(to[A-Z][A-Za-z0-9]*)\s*\(/g;
          // Test-block openers segment the file so the assert-after-action check
          // cannot pair an action in one test with an assertion in a different one.
          // ONLY real per-test declarations are boundaries — `test`/`it` and the
          // per-test modifiers only/skip/fixme/fail. NOT test.step() (a sub-step
          // WITHIN a test — an action and its assertion may sit in separate steps),
          // NOT test.describe()/beforeEach()/use()/… (groups and hooks).
          const TEST_OPENER_RE = /\b(?:test|it)(?:\s*\.\s*(?:only|skip|fixme|fail))?\s*\(/g;

          // Blank out comment AND string/template/regex contents in a single
          // left-to-right pass so the regexes above only ever see executable code —
          // a `.click(` or `.toHaveText(` inside a test title, fixture value, or
          // regex literal must NOT count as a real call. Conversely, code inside a
          // template placeholder (`${ await btn.click() }`) IS executable and must
          // be kept. This is a small JS lexer:
          //   - one pass, so a `//` inside a string ("http://…") isn't a comment;
          //   - `${…}` in a template re-enters code state (brace-depth tracked for
          //     nested `{}`), so interactions/assertions inside it still count;
          //   - regex literals are recognised (only where a `/` can legally begin
          //     one — not division) and their bodies blanked, incl. `[…]` classes;
          //   - newlines preserved so error line numbers stay meaningful.
          // `prev` is the last non-space CODE char, used to tell a regex `/` from a
          // division `/`: a regex cannot follow a value (identifier/digit/`)`/`]`).
          function sanitize(src) {
            let out = "";
            const blank = (ch) => { out += ch === "\n" ? "\n" : ch === "\t" ? "\t" : " "; };
            const stack = [{ t: "code", fromTpl: false, depth: 0 }];
            let prev = "";
            const regexOk = (p) => p === "" || !/[A-Za-z0-9_$)\]]/.test(p);
            for (let i = 0; i < src.length; i++) {
              const c = src[i], d = src[i + 1];
              const top = stack[stack.length - 1];
              if (top.t === "code") {
                if (c === "/" && d === "/") { stack.push({ t: "line" }); blank(c); blank(d); i++; continue; }
                if (c === "/" && d === "*") { stack.push({ t: "block" }); blank(c); blank(d); i++; continue; }
                if (c === "'") { stack.push({ t: "sq" }); blank(c); continue; }
                if (c === '"') { stack.push({ t: "dq" }); blank(c); continue; }
                if (c === "`") { stack.push({ t: "tpl" }); blank(c); continue; }
                if (c === "/" && regexOk(prev)) { stack.push({ t: "regex", cls: false }); blank(c); continue; }
                if (c === "{") { top.depth++; out += c; prev = "{"; continue; }
                if (c === "}") {
                  if (top.fromTpl && top.depth === 0) { stack.pop(); blank(c); continue; }
                  if (top.depth > 0) top.depth--;
                  out += c; prev = "}"; continue;
                }
                out += c;
                if (!/\s/.test(c)) prev = c;
                continue;
              }
              if (top.t === "line") {
                if (c === "\n") { stack.pop(); out += "\n"; } else { blank(c); }
                continue;
              }
              if (top.t === "block") {
                if (c === "*" && d === "/") { stack.pop(); blank(c); blank(d); i++; } else { blank(c); }
                continue;
              }
              if (top.t === "sq" || top.t === "dq") {
                const q = top.t === "sq" ? "'" : '"';
                if (c === "\\") { blank(c); blank(d); i++; continue; }
                if (c === q) { stack.pop(); blank(c); prev = "x"; continue; }
                blank(c); continue;
              }
              if (top.t === "tpl") {
                if (c === "\\") { blank(c); blank(d); i++; continue; }
                if (c === "`") { stack.pop(); blank(c); prev = "x"; continue; }
                if (c === "$" && d === "{") { stack.push({ t: "code", fromTpl: true, depth: 0 }); blank(c); blank(d); i++; continue; }
                blank(c); continue;
              }
              // regex literal
              if (c === "\\") { blank(c); blank(d); i++; continue; }
              if (top.cls) { if (c === "]") top.cls = false; blank(c); continue; }
              if (c === "[") { top.cls = true; blank(c); continue; }
              if (c === "\n") { stack.pop(); out += "\n"; continue; } // unterminated — bail
              if (c === "/") { stack.pop(); blank(c); prev = "x"; continue; }
              blank(c);
            }
            return out;
          }

          function scanAll(re, code) {
            const hits = [];
            re.lastIndex = 0;
            let m;
            while ((m = re.exec(code))) hits.push(m);
            return hits;
          }

          function analyze(src) {
            const code = sanitize(src);
            const matchers = scanAll(MATCHER_RE, code)
              .filter((m) => KNOWN_MATCHERS.has(m[2]))
              .map((m) => ({ name: m[2], negated: Boolean(m[1]), idx: m.index }));
            const actions = scanAll(ACTION_RE, code).map((m) => m.index);

            // Segment [start, end) per test block; matchers/actions before the first
            // test (imports, describe, hooks) sit in a leading segment ignored by the
            // ordering rule but still counted for the existence rules below.
            const opens = scanAll(TEST_OPENER_RE, code).map((m) => m.index);
            const segments = opens.length
              ? opens.map((s, k) => [s, k + 1 < opens.length ? opens[k + 1] : code.length])
              : [[0, code.length]];
            const assertAfterAction = segments.some(([s, e]) => {
              const acts = actions.filter((x) => x >= s && x < e);
              if (!acts.length) return false;
              const firstAct = Math.min(...acts);
              return matchers.some((x) => x.idx > firstAct && x.idx < e);
            });

            return {
              assertionCount: matchers.length,
              hasAction: actions.length > 0,
              hasBehavioral: matchers.some((x) => x.negated || !RENDER_ONLY.has(x.name)),
              assertAfterAction,
            };
          }

          const isExempt = (f) => EXEMPT.some((x) => f.includes(x));
          const weak = new Map(); // spec -> reasons[]
          for (const sf of specFiles) {
            if (isExempt(sf)) continue;
            let src;
            try {
              src = readFileSync(sf, "utf8");
            } catch {
              weak.set(sf, ["unreadable"]);
              continue;
            }
            const r = analyze(src);
            const reasons = [];
            if (!r.hasAction) reasons.push("no interaction (click/fill/press/selectOption/…) — a nav-only render smoke test");
            if (!r.hasBehavioral) reasons.push("no behavioral assertion — only toBeVisible/toBeHidden-style render checks");
            if (!r.assertAfterAction) reasons.push("no assertion after an interaction — nothing verifies a state change");
            if (r.assertionCount < MIN_ASSERT) reasons.push(`only ${r.assertionCount} assertion(s), need ≥ ${MIN_ASSERT}`);
            if (reasons.length) weak.set(sf, reasons);
          }

          const failures = [];
          // (1) every non-exempt spec must clear the bar, unless grandfathered.
          for (const [sf, reasons] of weak) {
            if (exSet.has(sf)) continue;
            failures.push(`${sf}: weak e2e spec — ${reasons.join("; ")}. Strengthen it, or grandfather it in ${exFile}`);
          }
          // (2) exceptions stay honest: a listed spec that no longer exists, or
          // now clears the bar, must be removed (drives the shrink).
          const specSet = new Set(specFiles);
          for (const ex of exSet) {
            if (!specSet.has(ex)) {
              failures.push(`${exFile}: "${ex}" is not a ${SUFFIX} spec — remove it`);
            } else if (!weak.has(ex)) {
              failures.push(`${exFile}: "${ex}" now meets the content bar — remove it (the list only shrinks)`);
            }
          }

          for (const f of failures) console.error(`::error::${f}`);
          if (failures.length > 0) {
            console.error(`${failures.length} e2e content-quality problem(s).`);
            process.exit(1);
          }
          console.log(`OK: ${specFiles.length} ${SUFFIX} spec(s) scanned; ${weak.size} weak (all grandfathered); ${exSet.size} listed.`);
          EOF
      - name: Enforce exceptions ratchet (shrink-only + touch-must-fix)
        if: ${{ !cancelled() && steps.install.outcome == 'success' && inputs.e2e-content-app-dirs != '' }}
        env:
          APP_DIRS: ${{ inputs.e2e-content-app-dirs }}
          SPEC_SUFFIX: ${{ inputs.e2e-content-spec-suffix }}
          EXEMPT: ${{ inputs.e2e-content-exempt-globs }}
          MIN_ASSERT: ${{ inputs.e2e-content-min-assertions }}
          EXCEPTIONS_FILE: ${{ inputs.e2e-content-exceptions }}
          BASE_REF: ${{ github.base_ref || 'main' }}
        shell: bash
        run: |
          set -o pipefail
          if [ -z "$EXCEPTIONS_FILE" ] || [ ! -f "$EXCEPTIONS_FILE" ]; then
            echo "No $EXCEPTIONS_FILE — nothing to ratchet."
            exit 0
          fi
          git fetch --no-tags origin "$BASE_REF"
          merge_base="$(git merge-base "origin/$BASE_REF" HEAD)" || {
            echo "::error::Could not compute merge-base with origin/$BASE_REF"
            exit 1
          }
          # Distinguish "absent at base" (this PR seeds the baseline — don't
          # reject the seeded entries) from "present at base" (enforce shrink-only).
          if git show "$merge_base:$EXCEPTIONS_FILE" > /tmp/e2e-content-base.json 2>/dev/null; then
            BASE_EXISTED=1
          else
            echo "[]" > /tmp/e2e-content-base.json
            BASE_EXISTED=0
          fi
          BASE_JSON=/tmp/e2e-content-base.json BASE_EXISTED="$BASE_EXISTED" HEAD_FILE="$EXCEPTIONS_FILE" MERGE_BASE="$merge_base" node <<'EOF'
          const { readFileSync } = require("node:fs");
          const { execSync } = require("node:child_process");
          const norm = (p) => String(p).replace(/\/+$/, "");
          const load = (f) => {
            try { const v = JSON.parse(readFileSync(f, "utf8")); return Array.isArray(v) ? v.map(norm) : []; }
            catch { return []; }
          };
          const base = new Set(load(process.env.BASE_JSON));
          const head = load(process.env.HEAD_FILE);
          let fail = false;

          // (A) shrink-only: no entry may be ADDED vs base — EXCEPT on the PR
          // that first creates the file (which establishes the baseline).
          if (process.env.BASE_EXISTED === "1") {
            const added = head.filter((p) => !base.has(p));
            if (added.length) {
              console.error("::error::" + process.env.HEAD_FILE + " may only shrink, never grow. Added:");
              added.forEach((p) => console.error(`  + ${p}`));
              console.error("You cannot grandfather a weak spec — strengthen it instead of listing it.");
              fail = true;
            }
          } else {
            console.log(`(initial ${process.env.HEAD_FILE} — establishing the grandfather baseline; shrink-only enforced from the next PR.)`);
          }

          // (B) touch-must-fix: a changed listed spec must be de-listed (i.e.
          // strengthened). A rename/delete counts as touching (check both sides).
          const listed = head;
          const mb = process.env.MERGE_BASE;
          const diff = execSync(`git diff --name-status -M --diff-filter=ACMRD ${mb} HEAD`, { encoding: "utf8" });
          const touched = new Set();
          for (const line of diff.split("\n")) {
            if (!line.trim()) continue;
            const parts = line.split("\t");
            const status = parts[0];
            const paths = status.startsWith("R") || status.startsWith("C") ? [parts[1], parts[2]] : [parts[1]];
            for (const p of paths) if (p) touched.add(p);
          }
          const offenders = listed.filter((sf) => touched.has(sf));
          if (offenders.length) {
            console.error("::error::You changed specs still grandfathered in " + process.env.HEAD_FILE + " (a rename or delete counts as touching):");
            offenders.forEach((s) => console.error(`  - ${s}`));
            console.error("Policy: a touched spec must be removed from the exceptions file AND strengthened to clear the content bar.");
            fail = true;
          }

          if (fail) process.exit(1);
          console.log(`✓ ${process.env.HEAD_FILE} ratchet OK (shrink-only; no touched spec left grandfathered).`);
          EOF

  e2e-affected:
    name: E2E (affected only)
    if: ${{ inputs.run-affected-e2e }}
    runs-on: ubuntu-latest
    env:
      USE_FILE_DB: '1'
      AUTH_SECRET: e2e-secret
      SKIP_ENV_VALIDATION: '1'
      E2E_AFFECTED_BASE: origin/${{ github.base_ref || 'main' }}
    steps:
      - uses: actions/checkout@v4
        with:
          # Full history so the selector can diff the branch against its base.
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
        with:
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node-version }}
          cache: 'pnpm'
      - name: Install dependencies
        shell: bash
        run: |
          set -o pipefail
          pnpm install --frozen-lockfile 2>&1 | tee install.log
      - name: Pre-e2e setup
        if: ${{ inputs.pre-e2e-command != '' }}
        shell: bash
        run: |
          set -o pipefail
          ${{ inputs.pre-e2e-command }} 2>&1 | tee pre-e2e.log
      - name: Resolve Playwright version
        id: playwright-version
        if: ${{ inputs.install-playwright }}
        shell: bash
        run: echo "version=$(node -p "require('@playwright/test/package.json').version")" >> "$GITHUB_OUTPUT"
      - name: Cache Playwright browsers
        id: playwright-cache
        if: ${{ inputs.install-playwright }}
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-${{ runner.arch }}-${{ steps.playwright-version.outputs.version }}
      - name: Install Playwright
        if: ${{ inputs.install-playwright && steps.playwright-cache.outputs.cache-hit != 'true' }}
        run: pnpm exec playwright install --with-deps chromium
      # A cache hit restores the browser binaries but NOT the apt system deps
      # (they live outside ~/.cache/ms-playwright), so install those alone.
      - name: Install Playwright system deps
        if: ${{ inputs.install-playwright && steps.playwright-cache.outputs.cache-hit == 'true' }}
        run: pnpm exec playwright install-deps chromium
      # ── Failed-first ordering (test prioritization) ───────────────────────
      # Playwright records each run's outcome in test-results/.last-run.json
      # (including the IDs of failed tests). Persisting it across runs lets the
      # consumer's selector run a fast `--last-failed` phase FIRST and bail
      # before the full affected set when a previous failure still fails — the
      # e2e analogue of vitest's failed-first sequencing. Restore is per-ref
      # only: another branch's failures are noise here, and this job never runs
      # on the default branch, so there is no base-branch cache to fall back to.
      # Harmless for consumers whose selector ignores the file (restore misses
      # or the phase never triggers).
      - name: Restore last-run results (failed-first ordering)
        uses: actions/cache/restore@v4
        with:
          path: test-results/.last-run.json
          key: e2e-last-run-${{ github.ref }}-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: |
            e2e-last-run-${{ github.ref }}-
      # Record the restored file's hash so the save below can skip when the run
      # didn't change it. On a stable green ref .last-run.json is a
      # deterministic {"status":"passed","failedTests":[]}, so without this
      # every run would add one immutable cache entry that says nothing new —
      # entries accumulate only while failures are actually changing, which is
      # when the cache is earning its keep. (hashFiles('' ) of a missing file
      # is the empty string, which also makes the first-ever save fire.)
      - name: Note restored last-run hash
        id: lastrun-pre
        shell: bash
        env:
          PRE_HASH: ${{ hashFiles('test-results/.last-run.json') }}
        run: echo "hash=$PRE_HASH" >> "$GITHUB_OUTPUT"
      # Runs only the specs the diff affects; full suite on shared/unmapped
      # changes; nothing on docs-only. Self-skips (exit 0) when nothing is affected.
      - name: Run affected e2e specs
        shell: bash
        run: |
          set -o pipefail
          git fetch --no-tags --depth=1 origin "${GITHUB_BASE_REF:-main}"
          pnpm test:e2e:affected 2>&1 | tee e2e-affected.log
      # Save even when the run FAILS — the failures are what the next run's
      # failed-first phase re-hits (actions/cache's built-in post step skips
      # saving on job failure, hence the explicit restore/save split). Guards:
      # the file must exist (a run that skipped e2e, or a consumer that never
      # produces it, has nothing to save — and cache/save errors on a missing
      # path) AND must differ from what was restored (unchanged results would
      # add a redundant immutable entry per run; see lastrun-pre). The
      # run_id/run_attempt suffix keeps every save key unique.
      - name: Save last-run results
        if: ${{ always() && hashFiles('test-results/.last-run.json') != '' && hashFiles('test-results/.last-run.json') != steps.lastrun-pre.outputs.hash }}
        uses: actions/cache/save@v4
        with:
          path: test-results/.last-run.json
          key: e2e-last-run-${{ github.ref }}-${{ github.run_id }}-${{ github.run_attempt }}

  e2e-reliability:
    name: E2E Reliability
    if: ${{ inputs.run-e2e-reliability }}
    runs-on: ubuntu-latest
    env:
      # In-process PGlite test DB (matches the consumer's e2e defaults). Override
      # in the consumer if it runs e2e differently.
      USE_FILE_DB: '1'
      AUTH_SECRET: e2e-secret
      SKIP_ENV_VALIDATION: '1'
      E2E_RELIABILITY_REPEAT: ${{ inputs.e2e-repeat }}
      E2E_RELIABILITY_BASE: origin/${{ github.base_ref || 'main' }}
    steps:
      - uses: actions/checkout@v4
        with:
          # Full history so the script can diff the branch against its base.
          fetch-depth: 0
      # Early exit BEFORE the toolchain: the consumer's reliability script
      # already self-skips when no e2e specs changed — but only after ~60s of
      # pnpm/node/Playwright installs. Most pushes change no spec, so detect
      # that case from the bare checkout and skip every later step: the job
      # then costs a checkout instead of a full setup. The pattern is a
      # SUPERSET of the consumer's own filter (see e2e-spec-pattern) — a false
      # positive only pays the setup, the script still decides precisely.
      - name: Detect changed e2e specs
        id: changed-specs
        shell: bash
        env:
          SPEC_PATTERN: ${{ inputs.e2e-spec-pattern }}
        run: |
          set -euo pipefail
          git fetch --no-tags --depth=1 origin "${GITHUB_BASE_REF:-main}"
          changed=$(git diff --name-only --diff-filter=d "FETCH_HEAD...HEAD" | grep -E "$SPEC_PATTERN" || true)
          if [ -n "$changed" ]; then
            echo "any=true" >> "$GITHUB_OUTPUT"
            echo "changed e2e specs:"
            echo "$changed"
          else
            echo "any=false" >> "$GITHUB_OUTPUT"
            echo "no e2e specs changed vs base — skipping toolchain setup and reliability run."
          fi
      - uses: pnpm/action-setup@v4
        if: ${{ steps.changed-specs.outputs.any == 'true' }}
        with:
          run_install: false
      - uses: actions/setup-node@v4
        if: ${{ steps.changed-specs.outputs.any == 'true' }}
        with:
          node-version: ${{ inputs.node-version }}
          cache: 'pnpm'
      - name: Install dependencies
        if: ${{ steps.changed-specs.outputs.any == 'true' }}
        shell: bash
        run: |
          set -o pipefail
          pnpm install --frozen-lockfile 2>&1 | tee install.log
      - name: Pre-e2e setup
        if: ${{ steps.changed-specs.outputs.any == 'true' && inputs.pre-e2e-command != '' }}
        shell: bash
        run: |
          set -o pipefail
          ${{ inputs.pre-e2e-command }} 2>&1 | tee pre-e2e.log
      - name: Resolve Playwright version
        id: playwright-version
        if: ${{ steps.changed-specs.outputs.any == 'true' && inputs.install-playwright }}
        shell: bash
        run: echo "version=$(node -p "require('@playwright/test/package.json').version")" >> "$GITHUB_OUTPUT"
      - name: Cache Playwright browsers
        id: playwright-cache
        if: ${{ steps.changed-specs.outputs.any == 'true' && inputs.install-playwright }}
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-${{ runner.arch }}-${{ steps.playwright-version.outputs.version }}
      - name: Install Playwright
        if: ${{ steps.changed-specs.outputs.any == 'true' && inputs.install-playwright && steps.playwright-cache.outputs.cache-hit != 'true' }}
        run: pnpm exec playwright install --with-deps chromium
      # A cache hit restores the browser binaries but NOT the apt system deps
      # (they live outside ~/.cache/ms-playwright), so install those alone.
      - name: Install Playwright system deps
        if: ${{ steps.changed-specs.outputs.any == 'true' && inputs.install-playwright && steps.playwright-cache.outputs.cache-hit == 'true' }}
        run: pnpm exec playwright install-deps chromium
      # Self-skips (exit 0) when no e2e specs changed vs the base branch — the
      # authoritative, consumer-side decision (the early exit above is only a
      # coarse superset).
      - name: Re-run changed e2e specs (retries=0)
        if: ${{ steps.changed-specs.outputs.any == 'true' }}
        shell: bash
        run: |
          set -o pipefail
          git fetch --no-tags --depth=1 origin "${GITHUB_BASE_REF:-main}"
          pnpm test:e2e:reliability 2>&1 | tee e2e-reliability.log
